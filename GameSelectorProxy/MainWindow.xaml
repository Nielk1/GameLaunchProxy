<Controls:MetroWindow x:Class="GameSelectorProxy.MainWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                      Title="MainWindow"
                      Height="335.627"
                      Width="499.235" WindowStyle="None" ShowTitleBar="False" ShowMaxRestoreButton="False" ShowMinButton="False" ShowCloseButton="False" ShowIconOnTitleBar="False">
    <Controls:MetroWindow.Resources>

    </Controls:MetroWindow.Resources>
    <Grid>
        <ListBox SelectedIndex="0">
            <Button Click="Close">XX1</Button>
            <Button Click="Close">XX2</Button>
            <Button Click="Close">Cancel</Button>
        </ListBox>
    </Grid>
</Controls:MetroWindow>